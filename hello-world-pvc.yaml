kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: hello-world-pvc
  labels:
    type: local
spec:
  storageClassName: hello-world
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
